import { Flex } from "@chakra-ui/react";

export const Projects = () => {
  return (
    <Flex
      h={"100vh"}
      w={"full"}
      bgGradient={"linear(to-b, blue.200, white)"}
    ></Flex>
  );
};
